### Run
```
npm install
npm start
```

### Tunnel
- Use VS Codes integrated port forwarding (https://code.visualstudio.com/docs/editor/port-forwarding)
- Forward the configured port (default: `3000`)
- Set port visibility to `public`